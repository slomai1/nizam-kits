export const meta = {
  name: 'auto-verify',
  description: 'اختبار تلقائي + تصحيح ذاتي: يكتشف التغييرات ← يختبر ← يصور النتيجة ← DeepSeek يصلح أخطاءه بنفسه',
  phases: [
    { title: 'اكتشاف', detail: 'تحديد الملفات المتغيرة ونوعها' },
    { title: 'اختبار', detail: 'تشغيل الاختبارات المناسبة لكل نوع ملف' },
    { title: 'تصحيح', detail: 'DeepSeek يصحح الأخطاء تلقائياً' },
    { title: 'تقرير', detail: 'نتائج نهائية مع لقطات شاشة' }
  ]
}

const TARGET = typeof args === 'string' ? args : (process.env.TARGET || '.')

phase('اكتشاف')
const changes = await agent(`حدد الملفات المتغيرة في: ${TARGET}.
شغّل:
- git diff --name-only
- git diff --stat

لكل ملف، حدد نوعه:
- 🎨 UI (tsx, jsx, vue, svelte, css, scss)
- ⚙️ منطق (ts, js, py, go, rs, php)
- 📝 وثائق/تكوين (md, json, yaml, config)

أخرج قائمة JSON:
{
  "ui": [{ "file": "...", "type": "react-component|css|..." }],
  "logic": [{ "file": "...", "type": "api-route|function|..." }],
  "docs": [{ "file": "..." }]
}`, { label: 'اكتشاف التغييرات' })

phase('اختبار')
const testResults = await agent(`اختبر التغييرات حسب نوع الملف:

### لملفات UI:
استخدم playwright MCP لـ:
1. افتح المتصفح على الصفحة المعنية
2. التقط لقطة شاشة (screenshot)
3. تحقق من عدم وجود أخطاء console
4. تحقق من ظهور العناصر المعدلة

### لملفات المنطق:
1. تحقق من الـ LSP diagnostics (استخدم getDiagnostics)
2. شغّل أي اختبارات موجودة (npm test, pytest, etc.)
3. تأكد من عدم وجود أخطاء type

### لملفات الوثائق:
1. تحقق من صحة الروابط
2. تأكد من التنسيق

التغييرات: ${changes || 'لم تكتشف تغييرات'}
أخرج النتائج مع وصف دقيق لكل خطأ.', { label: 'تنفيذ الاختبارات' })

phase('تصحيح')
const corrections = await agent(`حلل نتائج الاختبارات وصحح الأخطاء:

نتائج الاختبار: ${testResults || 'لا توجد نتائج'}
التغييرات الأصلية: ${changes || 'غير متوفرة'}

لكل نتيجة تحقق، صنّفها أولاً إلى أحد خمسة، ثم نفّذ الفعل المقابل — لا تتجاوز التصنيف:

1. **موثوق** (نجح) → استمر للخطوة التالية.
2. **قابل للتشخيص** (فشل وسببه واضح) → أعد المحاولة حاملاً السبب، لا إعادة فارغة.
3. **تعارض مسارات** (مساران يتصادمان) → خذ مساراً مستقلاً ثم قارن النتيجتين.
4. **لا قيد جديد** (الاستنتاج لا يضيف معلومات) → تحوّل لمرشّحين محدودين واختبار تفاضلي.
5. **انحلال** (النتائج تسوء بلا فائدة) → ارجع لآخر حالة متحقّق منها وأعد منها.

قاعدة: لا تكرر نفس الفعل الفاشل كما هو. بعد 3 محاولات من نفس التصنيف دون تقدم، قل "🛑 يحتاج تدخل بشري" واشرح السبب والتصنيف.

**مهم**: لا تعدل ملفات غير متعلقة بالخطأ.`, { label: 'تصحيح ذاتي' })

phase('تقرير')
log('# 🔄 تقرير التحقق التلقائي')
log(`**الهدف**: ${TARGET}`)
log('')
log('## 📁 التغييرات')
log(changes || 'لا توجد تغييرات')
log('')
log('## 🧪 نتائج الاختبارات')
log(testResults || 'لا توجد نتائج')
log('')
log('## 🔧 التصحيحات')
log(corrections || 'لا توجد تصحيحات')
log('')
log('---')
log('🔄 اكتمل التحقق التلقائي')
